<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul class="admin">
        <% data.forEach(item => { %>
            <li><%= item.name %> </li>
            <li> <%= item.phone %></li>
          <% }); %>
    </ul>
</body>
</html>